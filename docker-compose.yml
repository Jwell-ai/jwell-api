services:
  jwell-api:
    image: registry.digitalocean.com/alphart/alphart:jwell-api-latest
    pull_policy: always
    restart: unless-stopped
    network_mode: host
    env_file: .env
    volumes:
      - ./data:/data
      - ./logs:/app/logs
    command: ["--log-dir", "/app/logs"]
